<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>To Do List</title>
    <link href="/style.css" rel="stylesheet">
    <link rel="stylesheet" href="font-awesome/css/font-awesome.min.css">
  </head>
  <body>
    <div class="create">
      <h1>To Do List</h1>
      <form action="/createTodo/{{todo}}" method="post">
        <p>I need to...</p>
        <div class="create-todo-input">
          <input type="text" name="todo" placeholder="Pick up birthday cake..." />
          <button type="submit"><i class="fa fa-plus" aria-hidden="true"></i> Add Todo</button>
        </div>
      </form>
    </div>
    <div class="pending">
      <h2>Todos</h2>
      <ul>
        {{#incomplete}}
          <li>
            <form action="/markComplete/{{id}}" method="post">
              <button type="submit"><i class="fa fa-check" aria-hidden="true"></i></button> {{item}}
            </form>
          </li>
        {{/incomplete}}
        {{^incomplete}}
          <p>Hooray! There's nothing here for you to do yet.</p>
        {{/incomplete}}
      </ul>
    </div>
    <div class="completed">
      <h2>Complete</h2>
      <ul>
        {{#complete}}
          <li>
            <form action="/markIncomplete/{{id}}" method="post">
              <button type="submit"><i class="fa fa-check" aria-hidden="true"></i></button> <del>{{item}}</del>
            </form>
          </li>
        {{/complete}}
        {{^complete}}
          <p>You've either got some catching up to do or you can add a todo on top.</p>
        {{/complete}}
      </ul>
    </div>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
  </body>
</html>

<!-- Latest compiled and minified JavaScript -->
